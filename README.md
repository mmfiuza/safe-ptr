# SafePtr (work in progress)
A C++ container type.

```
rm -rf build && cmake -S . -B build && cmake --build build && ./build/basic-usage
```

## To do:
- add resize()
- add find()
- add contains()
- add print()
